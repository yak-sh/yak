# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# npm droppings — this project has no node_modules
node_modules/

# agent worktrees parked inside the repo
.claude/worktrees/

# Operator scratch — wake-brief note-to-self (re-injected post-clear), not a tracked artifact
.operate/

# Materialized persona projection — generated by task sync (src/persona.ts), the
# adopted voice AGENTS.md/CLAUDE.md symlink into. This repo un-tracked it in
# 5fc7573; it regenerates every sync, so ignore it lest a stray `git add -A`
# commit the derived output.
.tasks/
/rust/target/
