{
  "name": "@yaks/effects",
  "version": "0.1.0",
  "exports": "./mod.ts",
  "license": "Apache-2.0",
  "description": "Runs isolated handlers for component creation, changes, and removal after a graph transaction commits, with an optional durable ledger.",
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.0"
  },
  "publish": {
    "exclude": [
      "**/*_test.ts",
      "./harness.ts",
      "./browser.json"
    ]
  }
}
