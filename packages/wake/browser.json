{
  "_": "The browser gate: @yaks/wake must type-check with only the web platform in scope — no Deno namespace, no Node types. `deno check --config packages/wake/browser.json packages/wake/mod.ts` is the check; it is part of `deno task check`.",
  "compilerOptions": {
    "lib": ["dom", "esnext"],
    "strict": true
  },
  "imports": {
    "@yaks/query": "../query/mod.ts",
    "@yaks/vocab": "../vocab/mod.ts",
    "@yaks/graph": "../graph/mod.ts",
    "croner": "jsr:@hexagon/croner@^10.0.1"
  }
}
