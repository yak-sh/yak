{
  "title": "wake",
  "$defs": {
    "wake": {
      "component": true,
      "type": "object",
      "kind": true,
      "prefix": "W",
      "description": "a scheduled time to come back to something",
      "$comment": "A wake carries no patch to apply when it fires: a rule's produce already writes the entity it matched. Patching a wake's target declaratively would need a rule that names that target.",
      "properties": {
        "at": {
          "type": "string",
          "format": "date-time",
          "description": "when it is next due; absent once a one-shot has fired, or once a recurrence has ended"
        },
        "target": {
          "type": "string",
          "ref": "entity",
          "death": "cascade",
          "description": "what the wake is about; when absent, the entity carrying it"
        },
        "note": {
          "type": "string",
          "description": "a line for whoever is woken: why the wake was set"
        },
        "every": {
          "type": "string",
          "description": "how it recurs: a duration (30m, 2h, 1d), five cron fields (0 9 * * 1-5), or @hourly/@daily/@weekly/@monthly; a cron line may end with an IANA time zone (0 9 * * 1-5 America/New_York), otherwise UTC"
        },
        "while": {
          "type": "array",
          "description": "how it recurs while something holds: {match, every} in order, a query over the whole store and the cadence it asks for; at each firing the first whose query finds anything sets the next instant, else every does, and with neither the wake sleeps until a write makes one hold",
          "items": {
            "type": "object",
            "properties": {
              "match": {
                "type": "string",
                "format": "query",
                "description": "holds while this query finds anything"
              },
              "every": {
                "type": "string",
                "description": "the cadence while it holds, written as wake.every is"
              }
            },
            "required": ["match", "every"]
          }
        }
      }
    },
    "fired": {
      "component": true,
      "type": "object",
      "description": "when a wake last fired",
      "properties": {
        "at": {
          "type": "string",
          "format": "date-time",
          "description": "the instant it fired"
        }
      }
    }
  }
}
