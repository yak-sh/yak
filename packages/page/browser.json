{
  "_": "The browser check: @yaks/page's browser-facing exports must type-check with only the web platform in scope — no Deno namespace, no Node types. The main entry point runs an external archiver as a child process, so only ./vocab is checked here — the part a browser imports. Run `deno task check:browser`.",
  "entries": [
    "./vocab"
  ],
  "compilerOptions": {
    "lib": [
      "dom",
      "esnext"
    ],
    "strict": true
  },
  "imports": {
    "@yaks/vocab": "../vocab/mod.ts"
  }
}
