{
  "$vocabulary": {
    "https://yaks.sh/vocab/core": true
  },
  "title": "page",
  "$defs": {
    "web": {
      "component": true,
      "type": "object",
      "kind": true,
      "before": [
        "doc"
      ],
      "properties": {
        "url": {
          "type": "string",
          "format": "uri",
          "identity": true,
          "description": "the canonical address — what the page IS, so the entity's id is derived from it and witnessing one page twice is one entity"
        },
        "frozen_at": {
          "type": "string",
          "format": "date-time",
          "stamped": true,
          "description": "when its bytes were captured, if they were"
        },
        "bytes": {
          "type": "string",
          "bare": false,
          "stamped": true,
          "description": "where the captured document is: its SHA-256 address in the host's blob store"
        }
      },
      "description": "a page as witnessed: the address it was at, and when and where its bytes were frozen"
    }
  }
}
