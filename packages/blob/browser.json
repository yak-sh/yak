{
  "_": "The browser gate: @yaks/blob must type-check with only the web platform in scope — no Deno namespace, no Node types. `deno check --config packages/blob/browser.json packages/blob/mod.ts` is the check; it is part of `deno task check`. The filesystem backend looks its runtime up rather than importing one, which is what keeps it inside this gate.",
  "compilerOptions": {
    "lib": ["dom", "esnext"],
    "strict": true
  },
  "imports": {
    "@yaks/query": "../query/mod.ts",
    "@yaks/vocab": "../vocab/mod.ts",
    "@yaks/graph": "../graph/mod.ts",
    "@yaks/sql": "../sql/mod.ts"
  }
}
