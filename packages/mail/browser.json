{
  "_": "The browser gate: @yaks/mail must type-check with only the web platform in scope — no Deno namespace, no Node types, no Cloudflare globals. `deno check --config packages/mail/browser.json packages/mail/mod.ts` is the check; it is part of `deno task check`.",
  "compilerOptions": {
    "lib": ["dom", "esnext"],
    "strict": true
  },
  "imports": {
    "@yaks/query": "../query/mod.ts",
    "@yaks/vocab": "../vocab/mod.ts",
    "@yaks/doc": "../doc/mod.ts",
    "@yaks/graph": "../graph/mod.ts",
    "@yaks/effects": "../effects/mod.ts"
  }
}
