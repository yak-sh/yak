{
  "name": "@yaks/mail",
  "version": "0.1.0",
  "exports": "./mod.ts",
  "license": "Apache-2.0",
  "description": "Represents letters and their delivery outcomes as graph components, sends them through an injected sender, and converts arrivals to bundles.",
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.0"
  },
  "publish": {
    "exclude": [
      "**/*_test.ts",
      "./harness.ts",
      "./browser.json",
      "./conform.ts",
      "./workers.json"
    ]
  }
}
