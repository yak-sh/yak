{
  "name": "@yaks/mail",
  "version": "0.0.0",
  "exports": "./mod.ts",
  "license": "Apache-2.0",
  "description": "Letters as entities: the mail component domain for a yaks graph — a letter, its recipient, its outcome, the sending effect, and the arrival of one.",
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.0"
  },
  "publish": {
    "exclude": [
      "**/*_test.ts",
      "./harness.ts",
      "./browser.json",
      "./conform.ts",
      "./workers.json"
    ]
  }
}
