{
  "name": "@yaks/canvas",
  "version": "0.0.0",
  "exports": "./mod.ts",
  "license": "Apache-2.0",
  "description": "The spatial UI as data: canvases, cards, pins, cameras and cursors as graph components, plus the framework-free geometry a renderer needs.",
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.0"
  },
  "publish": {
    "exclude": [
      "**/*_test.ts",
      "./browser.json"
    ]
  }
}
