{
  "$vocabulary": {
    "https://yak.sh/vocab/core": true,
    "https://yak.sh/vocab/id": true
  },
  "title": "tmux",
  "$defs": {
    "tmux": {
      "component": true,
      "type": "object",
      "kind": true,
      "properties": {
        "of": {
          "type": "string",
          "ref": "entity",
          "death": "keep",
          "description": "what is running in the pane — a session, a process, anything that runs"
        },
        "pane": {
          "type": "string",
          "description": "the target string tmux accepts: a pane id (`%42`), or `session:window.pane`"
        }
      },
      "description": "a terminal somebody can watch: one tmux pane, and what is running in it"
    }
  }
}
