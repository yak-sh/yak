{
  "_": "The browser gate: @yaks/client must type-check with only the web platform in scope — no Deno namespace, no Node types. `deno check --config packages/client/browser.json packages/client/mod.ts` is the check; it is part of `deno task check`.",
  "compilerOptions": {
    "lib": ["dom", "esnext"],
    "strict": true
  },
  "imports": {
    "@yaks/query": "../query/mod.ts",
    "@yaks/vocab": "../vocab/mod.ts",
    "@yaks/sql": "../sql/mod.ts",
    "@yaks/graph": "../graph/mod.ts",
    "@yaks/match": "../match/mod.ts",
    "@yaks/ram": "../ram/mod.ts",
    "@yaks/sync": "../sync/mod.ts"
  }
}
