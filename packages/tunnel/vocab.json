{
  "$vocabulary": {
    "https://yak.sh/vocab/core": true
  },
  "title": "tunnel",
  "$defs": {
    "tunnel": {
      "component": true,
      "type": "object",
      "wire": false,
      "description": "the Cloudflare Tunnel that links a machine to this entity, and the Workers VPC Service the link's gateway reaches it through",
      "properties": {
        "id": {
          "type": "string",
          "stamped": true,
          "description": "the tunnel's id in the account"
        },
        "service": {
          "type": "string",
          "stamped": true,
          "description": "the VPC Service's id, which the link's gateway is bound to"
        },
        "adopted": {
          "type": "boolean",
          "stamped": true,
          "description": "made outside whatever keeps this record, which only records it: unlinking forgets the two ids and never removes or rotates what they name"
        }
      }
    }
  }
}
