{
  "name": "@yaks/tunnel",
  "version": "0.2.1",
  "exports": {
    ".": "./mod.ts",
    "./vocab": "./vocab.ts",
    "./service": "./service.ts"
  },
  "license": "Apache-2.0",
  "description": "Links a machine to a hosted space through a Cloudflare Tunnel and a Workers VPC Service: the account calls that make one, and the connector that runs it on the machine.",
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.0"
  },
  "publish": {
    "exclude": [
      "**/*_test.ts"
    ]
  }
}
