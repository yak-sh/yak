{
  "_": "The browser gate: @yaks/git must type-check with only the web platform in scope — no Deno namespace, no Node types. `deno check --config packages/git/browser.json packages/git/mod.ts` is the check; it is part of `deno task check`. The one platform API this package uses is `crypto.subtle`, which every runtime has.",
  "compilerOptions": {
    "lib": ["dom", "esnext"],
    "strict": true
  },
  "imports": {
    "@yaks/query": "../query/mod.ts",
    "@yaks/vocab": "../vocab/mod.ts",
    "@yaks/graph": "../graph/mod.ts",
    "@yaks/blob": "../blob/mod.ts",
    "@yaks/edge": "../edge/mod.ts",
    "@yaks/key": "../key/mod.ts",
    "@yaks/sql": "../sql/mod.ts"
  }
}
