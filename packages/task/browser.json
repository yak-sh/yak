{
  "_": "The browser gate: @yaks/task must type-check with only the web platform in scope — no Deno namespace, no Node types. `deno check --config packages/task/browser.json packages/task/mod.ts` is the check; it is part of `deno task check`.",
  "compilerOptions": {
    "lib": ["dom", "esnext"],
    "strict": true
  },
  "imports": {
    "@yaks/query": "../query/mod.ts",
    "@yaks/vocab": "../vocab/mod.ts",
    "@yaks/graph": "../graph/mod.ts",
    "@yaks/sql": "../sql/mod.ts",
    "@yaks/edge": "../edge/mod.ts"
  }
}
