{
  "$vocabulary": {
    "https://yaks.sh/vocab/core": true,
    "https://yaks.sh/vocab/id": true,
    "https://yaks.sh/vocab/edge": true,
    "https://yaks.sh/vocab/blob": true,
    "https://yaks.sh/vocab/kernel": true
  },
  "title": "task",
  "$defs": {
    "accept": {
      "component": true,
      "type": "object",
      "properties": {
        "body": {
          "type": "string",
          "store": "blob",
          "bare": false,
          "description": "the conditions, in prose"
        }
      },
      "description": "what finishing this means \u2014 written when the work is asked for, not after"
    },
    "blocked": {
      "component": true,
      "type": "object",
      "description": "something outside the graph is in the way. A facet, never a status: a blocked task is still open work.",
      "properties": {
        "on": {
          "type": "string",
          "description": "what it is waiting on, said in words"
        },
        "since": {
          "type": "string",
          "format": "date-time",
          "stamped": true
        }
      }
    },
    "cancelled": {
      "component": true,
      "type": "object",
      "description": "the mark a called-off task wears",
      "properties": {
        "at": {
          "type": "string",
          "format": "date-time",
          "description": "when it was called off",
          "stamped": true
        },
        "by": {
          "type": "string",
          "ref": "entity",
          "death": "keep",
          "description": "who called it off \u2014 kept as history when that entity is deleted",
          "stamped": true
        },
        "reason": {
          "type": "string",
          "description": "why it was called off"
        },
        "via": {
          "type": "string",
          "ref": "entity",
          "death": "keep",
          "stamped": true,
          "description": "the instrument it was written through \u2014 the session or client the write came in on"
        }
      }
    },
    "completed": {
      "component": true,
      "type": "object",
      "index": [
        [
          "at"
        ]
      ],
      "description": "the mark a finished task wears",
      "properties": {
        "at": {
          "type": "string",
          "format": "date-time",
          "description": "when it was finished",
          "stamped": true
        },
        "by": {
          "type": "string",
          "ref": "entity",
          "death": "keep",
          "description": "who finished it \u2014 kept as history when that entity is deleted",
          "stamped": true
        },
        "via": {
          "type": "string",
          "ref": "entity",
          "death": "keep",
          "stamped": true,
          "description": "the instrument it was written through \u2014 the session or client the write came in on"
        }
      }
    },
    "contains": {
      "component": true,
      "type": "object",
      "relation": "contains",
      "description": "that task is part of this one",
      "properties": {}
    },
    "requires": {
      "component": true,
      "type": "object",
      "relation": "requires",
      "description": "this task waits for that one",
      "properties": {}
    },
    "task": {
      "component": true,
      "type": "object",
      "kind": true,
      "prefix": "T",
      "governed": true,
      "description": "a thing to do",
      "properties": {
        "status": {
          "type": "string",
          "enum": [
            "cancelled",
            "done",
            "open"
          ],
          "computed": true,
          "description": "where the task stands \u2014 read off the completed and cancelled marks, never written"
        }
      }
    },
    "statuses": {
      "description": "every word a task's derived status can be, the host's extra marks included: `wip` is what a task under a live claim reads as, contributed by whoever ships the claim (see MARKS).",
      "enum": [
        "open",
        "wip",
        "done",
        "cancelled"
      ]
    },
    "task_new": {
      "tool": true,
      "noun": "task",
      "verb": "new",
      "title": "File a task",
      "description": "Write down something to do: an entity wearing task{} and doc{title, body}, open by the only definition there is \u2014 it wears neither the completed nor the cancelled mark. Say where it is filed and it wears filed{} too.",
      "input": {
        "title": {
          "type": "string",
          "description": "what to do, in a line"
        },
        "body": {
          "type": "string",
          "description": "the irreducible ask and its pointers, as Markdown"
        },
        "project": {
          "type": "string",
          "ref": "project",
          "description": "the project it is filed under"
        },
        "priority": {
          "type": "number",
          "description": "queue order \u2014 lower is more urgent"
        },
        "assignee": {
          "type": "string",
          "ref": "entity",
          "description": "whose queue it is filed in"
        },
        "domain": {
          "type": "string",
          "description": "the work area"
        }
      },
      "required": [
        "title"
      ],
      "options": {
        "positional": [
          "title",
          "body"
        ],
        "short": {
          "p": "project",
          "b": "body"
        }
      }
    },
    "task_list": {
      "tool": true,
      "noun": "task",
      "verb": "list",
      "title": "The list",
      "description": "The tasks a query line selects, as whole bundles. `.task` is always on the line; what you say is joined onto it, and saying nothing means `.status=open`. The grammar is the query grammar: `.status=done`, `.filed.project=P-19`, `.priority<3`, a bare word for its text.",
      "input": {
        "query": {
          "type": "string",
          "description": "dot-params joined onto `.task`"
        },
        "limit": {
          "type": "number",
          "description": "at most this many"
        }
      },
      "options": {
        "positional": [
          "query"
        ],
        "short": {
          "n": "limit"
        }
      },
      "readOnly": true
    },
    "task_update": {
      "tool": true,
      "noun": "task",
      "verb": "update",
      "title": "Move a task",
      "description": "Change where a task stands or where it is filed. `status` is not a column: done adds the completed mark and drops the cancelled one, cancelled the other way about, and open drops both. Anything you leave out is left alone.",
      "input": {
        "task": {
          "type": "string",
          "ref": "task",
          "description": "the task to move"
        },
        "status": {
          "type": "string",
          "enum": [
            "open",
            "done",
            "cancelled"
          ],
          "description": "where it stands now"
        },
        "title": {
          "type": "string",
          "description": "a new title"
        },
        "body": {
          "type": "string",
          "description": "a new body"
        },
        "project": {
          "type": "string",
          "ref": "project",
          "description": "the project it is filed under"
        },
        "priority": {
          "type": "number",
          "description": "queue order \u2014 lower is more urgent"
        },
        "assignee": {
          "type": "string",
          "ref": "entity",
          "description": "whose queue it is filed in"
        },
        "domain": {
          "type": "string",
          "description": "the work area"
        }
      },
      "required": [
        "task"
      ],
      "options": {
        "positional": [
          "task",
          "status"
        ],
        "short": {
          "p": "project",
          "s": "status"
        }
      },
      "idempotent": true
    }
  }
}
