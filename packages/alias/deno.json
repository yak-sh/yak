{
  "name": "@yaks/alias",
  "version": "0.0.0",
  "exports": "./mod.ts",
  "license": "Apache-2.0",
  "description": "A name for an entity, worth as much as its id: the alias component domain for a yaks graph — write the same name twice and it lands on the same entity.",
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.0"
  },
  "publish": {
    "exclude": [
      "**/*_test.ts",
      "./harness.ts",
      "./browser.json"
    ]
  }
}
