/**
 * Content-addressed component-table sets, cached presence matching and moves,
 * and the graph plugin that maintains an entity's archetype. Table names, not
 * a reader's vocabulary, define identity. No query lowering or platform APIs.
 * @module
 */
export * from './sets.ts'
export * from './plugin.ts'
