{
  "$vocabulary": { "https://yaks.sh/vocab/core": true },
  "title": "archetype",
  "$defs": {
    "archetype": {
      "component": true,
      "type": "object",
      "kind": true,
      "description": "Immutable component-table set; its eid is derivedEid('archetype|' + bytewise-sorted table names joined with ',').",
      "properties": {
        "tables": {
          "type": "string",
          "format": "json",
          "description": "The canonical table-name list, encoded as a JSON array."
        }
      }
    },
    "retired": {
      "component": true,
      "type": "object",
      "description": "This archetype names a table that is no longer in the database file. Kept forever, never deleted."
    }
  }
}
