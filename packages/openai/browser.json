{
  "_": "The browser gate: @yaks/openai must type-check with only the web platform in scope — no Deno namespace, no Node types. `deno check --config packages/openai/browser.json packages/openai/mod.ts` is the check; it is part of `deno task check`.",
  "compilerOptions": {
    "lib": ["dom", "dom.asynciterable", "esnext"],
    "strict": true
  },
  "imports": {
    "@yaks/query": "../query/mod.ts",
    "@yaks/vocab": "../vocab/mod.ts",
    "@yaks/graph": "../graph/mod.ts",
    "@yaks/model": "../model/mod.ts"
  }
}
