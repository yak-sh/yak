{
  "$vocabulary": {
    "https://yak.sh/vocab/core": true,
    "https://yak.sh/vocab/key": true
  },
  "title": "key",
  "$defs": {
    "key": {
      "component": true,
      "type": "object",
      "kind": true,
      "description": "a value that identifies an entity; a second component beside it tags which kind of value it is",
      "properties": {
        "of": {
          "type": "string",
          "ref": "entity",
          "death": "release",
          "bare": false,
          "description": "the entity the value identifies; the row is removed when that entity is deleted, which frees the value"
        },
        "value": {
          "type": "string",
          "bare": false,
          "description": "the value itself; unique within its kind, because the key's own id is derived from the kind and the value"
        }
      }
    }
  }
}
