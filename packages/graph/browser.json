{
  "_": "The browser gate: @yaks/graph must type-check with only the web platform in scope — no Deno namespace, no Node types. `deno check --config packages/graph/browser.json packages/graph/mod.ts` is the check; it is part of `deno task check`.",
  "compilerOptions": {
    "lib": ["dom", "esnext"],
    "strict": true
  },
  "imports": {
    "@yaks/graph": "./mod.ts",
    "@yaks/match": "../match/mod.ts",
    "@yaks/query": "../query/mod.ts",
    "@yaks/sql": "../sql/mod.ts",
    "@yaks/vocab": "../vocab/mod.ts"
  }
}
