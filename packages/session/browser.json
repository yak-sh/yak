{
  "_": "The browser gate: @yaks/session must type-check with only the web platform in scope \u2014 no Deno namespace, no Node types. `deno check --config packages/session/browser.json packages/session/mod.ts` is the check; it is part of `deno task check`.",
  "compilerOptions": {
    "lib": [
      "dom",
      "esnext"
    ],
    "strict": true
  },
  "imports": {
    "@yaks/query": "../query/mod.ts",
    "@yaks/vocab": "../vocab/mod.ts",
    "@yaks/graph": "../graph/mod.ts",
    "@yaks/match": "../match/mod.ts",
    "@yaks/render": "../render/mod.ts",
    "@yaks/model": "../model/mod.ts",
    "@yaks/effects": "../effects/mod.ts",
    "@yaks/task": "../task/mod.ts",
    "@yaks/edge": "../edge/mod.ts",
    "@yaks/sql": "../sql/mod.ts",
    "@yaks/context": "../context/mod.ts"
  }
}
