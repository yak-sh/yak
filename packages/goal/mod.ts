/**
 * A purpose that is never finished: a `goal` is not a task and has no status —
 * work SATISFIES it. "Reduce noise, amplify signal" is a goal; the task that
 * cut a page of prose out of the graph satisfies it, and records that with a
 * `satisfies` link.
 *
 * Components only: a vocabulary document, with no code.
 */

export { goalDoc } from './vocab.ts'
