{
  "_": "The browser gate: @yaks/sync must type-check with only the web platform in scope — no Deno namespace, no Node types. `deno check --config packages/sync/browser.json packages/sync/mod.ts` is the check; it is part of `deno task check`.",
  "compilerOptions": {
    "lib": ["dom", "esnext"],
    "strict": true
  },
  "imports": {
    "@yaks/query": "../query/mod.ts",
    "@yaks/vocab": "../vocab/mod.ts",
    "@yaks/match": "../match/mod.ts",
    "@yaks/graph": "../graph/mod.ts"
  }
}
