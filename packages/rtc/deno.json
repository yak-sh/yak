{
  "name": "@yaks/rtc",
  "version": "0.2.5",
  "exports": {
    ".": "./mod.ts",
    "./door": "./door.ts",
    "./vocab": "./vocab.ts"
  },
  "license": "Apache-2.0",
  "description": "Voice between pages over Cloudflare Realtime: a page joins a session, publishes its microphone and hears the tracks it asks for, and a host answers the door that keeps the secret, the sessions and the meter.",
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.0"
  },
  "compilerOptions": {
    "lib": ["dom", "esnext"]
  },
  "publish": {
    "exclude": ["**/*_test.ts", "./browser.json"]
  }
}
