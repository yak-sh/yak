{
  "_": "The browser gate: @yaks/doc must type-check with only the web platform in scope — no Deno namespace, no Node types. `deno check --config packages/doc/browser.json packages/doc/mod.ts` is the check; it is part of `deno task check`.",
  "compilerOptions": {
    "lib": [
      "dom",
      "esnext"
    ],
    "strict": true
  },
  "imports": {
    "@yaks/query": "../query/mod.ts",
    "@yaks/vocab": "../vocab/mod.ts",
    "@yaks/graph": "../graph/mod.ts",
    "@yaks/render": "../render/mod.ts",
    "@yaks/markdown": "../markdown/mod.ts",
    "@yaks/match": "../match/mod.ts",
    "@yaks/archetype": "../archetype/mod.ts",
    "@yaks/sql": "../sql/mod.ts",
    "marked": "npm:marked@18.0.6",
    "preact": "npm:preact@^10.29.1"
  }
}
