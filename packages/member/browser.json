{
  "_": "The browser check: @yaks/member must type-check with only the web platform in scope — no Deno namespace, no Node types. Run `deno check --config packages/member/browser.json packages/member/mod.ts`; it is part of `deno task check`.",
  "compilerOptions": {
    "lib": ["dom", "esnext"],
    "strict": true
  },
  "imports": {
    "@yaks/query": "../query/mod.ts",
    "@yaks/vocab": "../vocab/mod.ts",
    "@yaks/graph": "../graph/mod.ts"
  }
}
