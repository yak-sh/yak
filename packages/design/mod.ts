/**
 * What was proposed and what was decided: a `design` is a proposal written down
 * so it can be argued with, a `review` is somebody's verdict on one, and an
 * `architecture` is the description that stands afterwards. The components
 * recording who proposed and who decided are [@yaks/kernel](../kernel)'s,
 * because anything can be proposed.
 *
 * Components only: a vocabulary document, no machinery.
 */

export { designDoc } from './vocab.ts'
