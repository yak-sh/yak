{
  "$vocabulary": {
    "https://yaks.sh/vocab/core": true
  },
  "title": "embedding",
  "$defs": {
    "vector_check": {
      "tool": true,
      "noun": "vector",
      "verb": "check",
      "title": "Check the vector index",
      "description": "Report whether the vector index is being rebuilt. A dirty flag older than the sweep’s own interval means no process is rebuilding the index, so every similarity search answers from the last good build. Nothing else reports this: writes still succeed and searches still return results.",
      "input": {},
      "readOnly": true
    }
  }
}
