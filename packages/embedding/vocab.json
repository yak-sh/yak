{
  "$vocabulary": {
    "https://yaks.sh/vocab/core": true
  },
  "title": "embedding",
  "$defs": {
    "vector_check": {
      "tool": true,
      "noun": "vector",
      "verb": "check",
      "title": "Check the vector index",
      "description": "Reports whether the vector index is still being rebuilt. A dirty flag older than the sweep's own interval means nothing is rebuilding the index, so every similarity search is answering from the last good build — silently, since writes still succeed and searches still return results.",
      "input": {},
      "readOnly": true
    }
  }
}
