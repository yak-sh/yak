// What a harness graph does after a commit, exported as
// `@yaks/harness/effects` beside `./rules`: the secrets plugin those rules
// install seals nothing on its own, and this is its seal (@yaks/secrets).

export { effects } from '@yaks/secrets/effects'
