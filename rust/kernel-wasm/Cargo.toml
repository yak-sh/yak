[package]
name = "kernel-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
kernel = { path = "../kernel", default-features = false }
serde_json = { version = "1", features = ["preserve_order"] }
