{
  "name": "yak",
  "private": true,
  "license": "FSL-1.1-ALv2",
  "description": "The npm dependencies of the kernel Worker and of the Workers its deploy deploys beside it (esbuild/, outbound/), bundled by wrangler; node_modules is gitignored, so nothing here runs by hand — `deno task deploy:yak` and `deno task dev:yak` (and the probe) install through wrangler.ts before wrangler reads it. Exact pins only. This file is for npm alone. NO `version` field: give it one and deno links the package for real, then refuses every check with `Linking npm packages requires using a node_modules directory`, since the kernel's own imports resolve through the root import map instead. Deno is kept away from this file by the deno.json beside it, which is what makes this directory a workspace member — without it deno knew the member only by this package.json and printed `Linked package ignored because package.json was missing version field` on every `task` invocation (T-35343). wrangler warns about none of this.",
  "dependencies": {
    "@cfworker/json-schema": "4.1.1",
    "@cloudflare/sandbox": "0.12.9",
    "@cloudflare/worker-bundler": "0.2.4",
    "@cloudflare/workers-oauth-provider": "0.10.3",
    "@modelcontextprotocol/sdk": "1.29.0",
    "@sentry/cloudflare": "10.75.0",
    "@sentry/core": "10.75.0",
    "@std/csv": "npm:@jsr/std__csv@1.0.6",
    "@std/yaml": "npm:@jsr/std__yaml@1.2.0",
    "croner": "10.0.1",
    "es-module-lexer": "2.3.2",
    "lucide": "1.31.0",
    "marked": "18.0.6",
    "postal-mime": "3.0.0",
    "preact": "10.29.7",
    "preact-render-to-string": "6.7.0",
    "semver": "7.8.5",
    "sucrase": "3.35.1",
    "zod": "3.25.76",
    "zod-to-json-schema": "3.25.2"
  },
  "devDependencies": {
    "wrangler": "4.111.0"
  }
}
