{
  "name": "tasks-fleet",
  "owner": { "name": "tasks" },
  "plugins": [
    {
      "name": "tasks",
      "source": "./tasks",
      "description": "Tasks channel — pushes things aimed at a session (a comment on its entity, a knock at its door) into the running Claude session as <channel source=\"tasks\"> events. Read-only listener over the tasks server's /ws sync socket."
    }
  ]
}
